<UserControl
    x:Class="HillsCafeManagement.Views.Cashier.Orders.OrdersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="#2A2B40">

    <UserControl.Resources>
        <XmlDataProvider x:Key="OrdersData" XPath="Orders">
            <x:XData>
                <Orders xmlns="">
                    <Order>
                        <Date>2025-07-31</Date>
                        <TableNumber>5</TableNumber>
                        <CashierName>Alex</CashierName>
                        <Status>Served</Status>
                    </Order>
                    <Order>
                        <Date>2025-07-30</Date>
                        <TableNumber>3</TableNumber>
                        <CashierName>Jamie</CashierName>
                        <Status>Pending</Status>
                    </Order>
                    <Order>
                        <Date>2025-07-29</Date>
                        <TableNumber>1</TableNumber>
                        <CashierName>Pat</CashierName>
                        <Status>Cancelled</Status>
                    </Order>
                </Orders>
            </x:XData>
        </XmlDataProvider>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="🛒 Orders Management"
                   FontSize="28"
                   FontWeight="Bold"
                   Foreground="White"
                   Margin="0,0,0,20"/>

        <!-- Search + Add -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Background="#D9D9D9" CornerRadius="10" Padding="8" Margin="0,0,10,0">
                <TextBox Background="Transparent"
                         BorderThickness="0"
                         Foreground="Black"
                         FontSize="14"
                         VerticalContentAlignment="Center" />
            </Border>

            <Button Content="Add Order"
                    Grid.Column="1"
                    Background="#6378FF"
                    Foreground="White"
                    FontWeight="Bold"
                    Padding="10,5"
                    Cursor="Hand"
                    BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Source={StaticResource OrdersData}, XPath=Order}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Background="#2A2B40"
                  Foreground="Black"
                  RowBackground="#D9D9D9"
                  AlternatingRowBackground="#D9D9D9"
                  BorderThickness="0"
                  GridLinesVisibility="None"
                  ColumnHeaderHeight="40"
                  RowHeight="40"
                  Margin="0,10,0,0">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#6378FF"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Info" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="ℹ️"
                                    ToolTip="View Order Info"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    FontSize="16"
                                    Cursor="Hand"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Date" Binding="{Binding XPath=Date}" Width="*" />
                <DataGridTextColumn Header="Table" Binding="{Binding XPath=TableNumber}" Width="*" />
                <DataGridTextColumn Header="Cashier" Binding="{Binding XPath=CashierName}" Width="*" />
                <DataGridTextColumn Header="Status" Binding="{Binding XPath=Status}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
