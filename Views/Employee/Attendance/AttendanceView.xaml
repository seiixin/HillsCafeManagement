<UserControl x:Class="HillsCafeManagement.Views.Employee.Attendance.AttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:HillsCafeManagement">

    <StackPanel Margin="20">
        <TextBlock Text="⏱ Clock In/Out Panel" FontSize="20" FontWeight="Bold" Margin="0 0 0 10" Foreground="white"/>
        <Button Content="Clock In" Command="{Binding ClockInCommand}" Margin="0 0 0 5"/>
        <Button Content="Clock Out" Command="{Binding ClockOutCommand}" Margin="0 0 0 20"/>
    </StackPanel>
</UserControl>